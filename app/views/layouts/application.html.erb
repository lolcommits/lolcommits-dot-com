<!DOCTYPE html>
<html>
<head>
  <title>Lolcommits</title>
  <%= stylesheet_link_tag "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <a href="https://github.com/lolcommits/lolcommits-dot-com">
    <img style="position: absolute; top: 35px; right: 0; border: 0;" src="https://s3.amazonaws.com/github/ribbons/forkme_right_darkblue_121621.png" alt="Fork me on GitHub">
  </a>
  <div class="navbar navbar-fixed-top">
    <div class="navbar-inner">
      <div class="container">
        <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </a>

        <%= link_to 'Lolcommits', root_url, :class => 'brand'%>
        <div class="nav-collapse" id="main-menu">
          <ul class="nav" id="main-menu-left">
            <% if current_user %>
              <% current_user.repos.each do |repo|%>
                <li>
                  <%= link_to repo.name, repo_path(repo) %>
                </li>
              <% end %>
              <li><%= link_to 'New Repo', new_repo_path %></li>
              <li><%= link_to 'Account Info', account_users_path %></li>
              <li><%= link_to 'My Profile', user_path(current_user) %></li>
              <li><%= link_to 'Logout', sessions_path, method: :delete %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </div>
  <div class="container" style="margin-top:60px;">
    <%= yield %>
  </div>
</body>
</html>
